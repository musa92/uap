# AdRequest

**Name** `dev.uap.supply.ad_request` · **Version** `2026-09-02` · **Schema** [`supply/ad_request.json`](https://uap.dev/schemas/supply/ad_request.json)

The request body a serving node sends to an exchange to fill one or more placements on a generated turn. Sent only in the hosted and hybrid decisioning profiles; Profile L makes no request at all.

> **Rationale.** Field-for-field mappable onto an OpenRTB 2.6 BidRequest so existing demand can bid with a translation layer rather than a rewrite. Each member names its OpenRTB equivalent. Where UAP has no equivalent the reason is an invariant, not an oversight:

```text
  BidRequest.site / .app     no addressable page exists; replaced by `inference`
  BidRequest.device          almost every member is a fingerprinting vector; see `surface`
  BidRequest.user            no cross-session identifier may exist at all (I1)
  Imp.banner / .video        creative is structured data, not a rendered size
```

> See SPEC.md Appendix C for the full mapping table.

## Properties

| Property | Type | Required | Description |
|---|---|---|---|
| `id` | string | yes | Unique identifier for this request, generated by the node. *(maxLength 128)* |
| `uap_version` | [`version`](uap.md) | yes |  |
| `test` | enum: `0`, `1` |  | 1 marks a test request that MUST NOT be billed. *(default 0)* |
| `integrity` | object | yes | The node's commitment to the organic answer, made before the auction runs. |
| `placements` | array of [`placement_request`](#definitions) | yes | The slots to fill. At least one is required. *(minItems 1, maxItems 8)* |
| `inference` | [`inference`](#definitions) | yes | What produced the turn: the model, its steward, and the serving stack. |
| `surface` | [`surface`](#definitions) |  | The rendering environment, described only to the extent that does not identify a device. |
| `context` | [`context_signal`](context_signal.md) |  | The bounded, conversation-derived signal. Absent means untargeted inventory. |
| `session` | [`session`](#definitions) |  | Turn position within the current conversation. Node-scoped and non-durable. |
| `auction` | [`auction`](#definitions) | yes |  |
| `policy` | [`policy`](#definitions) |  |  |
| `regs` | [`regs`](regs.md) |  |  |
| `source` | [`source`](#definitions) |  |  |

## Definitions

### `placement_request`

| Property | Type | Required | Description |
|---|---|---|---|
| `placement_id` | string | yes | Identifier for this slot, unique within the request. *(maxLength 128)* |
| `placement` | [`placement`](placement.md) | yes | The slot definition. |
| `tag_id` | string |  | The seller's own identifier for this inventory unit. *(maxLength 128)* |
| `floor_cpm_micros` | [`cpm`](cpm.md) |  | Minimum acceptable clearing price for this placement. |
| `currency` | [`currency`](currency.md) |  |  |
| `deals` | array of [`deal`](deal.md) |  | Pre-negotiated arrangements available on this placement. *(maxItems 32)* |
| `private_auction` | enum: `0`, `1` |  | 1 restricts this placement to the listed deals. *(default 0)* |
| `expires_in_s` | integer |  | Seconds within which the winning creative must be rendered. *(minimum 1, maximum 3600)* |
| `metrics` | array of object |  | Seller-supplied performance predictions for this placement. *(maxItems 16)* |

### `inference`

Replaces OpenRTB `site` and `app`. The unit of supply is a model turn, so the model is the inventory.

> **Rationale.** This object is what a buyer applies suitability policy against. On the open web that decision is made on a domain; here there is no domain, and the closest equivalent is which model produced the text the ad sits beside.

| Property | Type | Required | Description |
|---|---|---|---|
| `model_id` | string | yes | Identifier of the model serving the turn. *(maxLength 256)* |
| `weights_digest` | [`digest`](digest.md) |  | Digest of the weights in use, where the operator can attest to it. |
| `steward_id` | string |  | Entity whose /.well-known/uap-model governs monetisation of this model. *(maxLength 256)* |
| `serving_stack` | string |  | The inference server in use. *(maxLength 64)* |
| `quantization` | string |  | Quantization applied to the weights, where known. *(maxLength 32)* |
| `entity_id` | string |  | The serving node's own identifier. *(maxLength 256)* |
| `supply_agent_id` | string |  | Aggregator holding the commercial account, where one exists. *(maxLength 256)* |
| `trust_tier` | enum: `0`, `1`, `2` |  | Tier asserted by this supply. Verified at settlement, not at request time. |

### `surface`

Replaces OpenRTB `device`. Every member is a closed enumeration.

> **Rationale.** OpenRTB `device` carries ua, ip, ifa, model, make, and a screen geometry, which together identify a device even without a cookie. None of that may appear here. What remains is what a buyer genuinely needs to render correctly and nothing that narrows the audience below the k floor.

| Property | Type | Required | Description |
|---|---|---|---|
| `type` | enum: `chat`, `agent_tool_result`, `voice`, `ide`, `search_answer`, `embedded_widget` |  |  |
| `client` | enum: `web`, `mobile`, `desktop`, `cli`, `api`, `device` |  |  |
| `renderer` | enum: `markdown`, `plaintext`, `native`, `voice`, `structured` |  |  |
| `modality` | array of enum: `text`, `image`, `audio`, `video` |  | *(maxItems 4)* |
| `locale` | string `/^[a-z]{2}(-[A-Z]{2})?$/` |  |  |
| `geo` | object |  | Coarse location. Country granularity is the default and the maximum for most deployments. |
| `attested` | boolean |  | Whether the surface runs an attested build. Substantiated at settlement. |

### `session`

Position within the current conversation. Contains no identifier of any kind.

> **Rationale.** OpenRTB `user` has no equivalent here and cannot have one: I1 forbids any identifier stable across sessions, including a hash. The consequence is stated plainly rather than hidden. Frequency capping is enforceable only within a single serving node, because only the node can correlate turns, and it never transmits what it used to do so. A buyer reading a frequency cap in a campaign bundle should read it as per-node, not network-wide.

| Property | Type | Required | Description |
|---|---|---|---|
| `turn_index_bucket` | enum: `0`, `1`, `2-5`, `6+` |  |  |
| `is_followup` | boolean |  |  |
| `ads_shown_this_conversation` | integer |  | How many placements this conversation has already carried. Lets a buyer decline to add to a crowded turn. *(minimum 0, maximum 64)* |

### `auction`

| Property | Type | Required | Description |
|---|---|---|---|
| `mechanism` | enum: `uap.auction.second_price`, `uap.auction.first_price` | yes | Pricing rule the exchange will apply. |
| `currency` | [`currency`](currency.md) | yes |  |
| `timeout_ms` | integer | yes | Milliseconds the node will wait before treating the request as a no-fill. *(minimum 1, maximum 1000)* |
| `objective` | enum: `reach`, `clicks`, `conversions` |  | What the seller expects demand to optimise for on this request. |
| `seats_allowed` | array of string |  | *(maxItems 128)* |
| `seats_blocked` | array of string |  | *(maxItems 128)* |

### `policy`

Seller-side restrictions the exchange MUST apply before ranking.

| Property | Type | Required | Description |
|---|---|---|---|
| `blocked_advertisers` | array of string |  | *(maxItems 512)* |
| `blocked_categories` | array of string |  | *(maxItems 512)* |
| `category_taxonomy` | string |  | Taxonomy the category values are drawn from. *(maxLength 32, default "uap.category/1.0")* |
| `allowed_formats` | array of enum: `sponsored_link`, `sponsored_card`, `sponsored_suggestion`, `product_offer`, `sponsored_action` |  | *(maxItems 16)* |
| `brand_suitability` | object |  | Maximum acceptable risk per GARM content category. |

### `source`

Provenance of this request.

> **Rationale.** Maps to OpenRTB `source`.

| Property | Type | Required | Description |
|---|---|---|---|
| `transaction_id` | string |  | Identifier shared by every hop handling this impression opportunity. *(maxLength 128)* |
| `final_decision` | enum: `0`, `1` |  | 1 when this exchange makes the final selection, 0 when it is one of several. |
| `supply_chain` | [`supply_chain`](supply_chain.md) |  |  |
| `payment_chain` | string |  | Ordered list of entities in the payment path. *(maxLength 512)* |

---

*Generated from `source/schemas/supply/ad_request.json`. Do not edit; run `make docs`.*
